apiVersion: v1
kind: Service
metadata:
  name: aura
  namespace: media
  labels:
    app: aura
spec:
  selector:
    app: aura
  ports:
    - port: 3000
      targetPort: 3000
      name: http
    - port: 8888
      targetPort: 8888
      name: api
