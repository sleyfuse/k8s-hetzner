apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgresql
spec:
  instances: 2
  ephemeralVolumesSizeLimit:
    shm: 1Gi
  storage:
    size: 10Gi
  postgresql:
    parameters:
      dynamic_shared_memory_type: "sysv"
