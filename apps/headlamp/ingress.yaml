apiVersion: traefik.io/v1alpha1
kind: Ingress
metadata:
  name: headlamp
  namespace: headlamp
spec:
  rules:
    - match: Host(`headlamp.alixroden.co.uk`)
      kind: Rule
      services:
        - name: headlamp
          port: 8080
      middlewares:
        - name: traefik-forward-auth
