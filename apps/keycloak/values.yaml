keycloak:
  hostname: "https://auth.alixroden.co.uk"
  hostnameAdmin: "https://auth.alixroden.co.uk"
  username: "admin"
  password: "KXX14cdup$"
  proxyHeaders: "xforwarded"
persistence:
  enabled: true
  storageClass: proxmox-data-xfs
  accessMode: ReadWriteOnce
  size: 10Gi
tls:
  enabled: true
  certManager:
    enabled: true
    issuerRef:
      name: "letsencrypt"
      kind: ClusterIssuer
      namespace: cert-manager
    dnsNames:
      - auth.alixroden.co.uk
