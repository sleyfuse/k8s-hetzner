apiVersion: v1
kind: Service
metadata:
  name: profilarr
  namespace: media
  labels:
    app: profilarr
spec:
  selector:
    app: profilarr
  ports:
    - port: 6868
      targetPort: 6868
      name: http
