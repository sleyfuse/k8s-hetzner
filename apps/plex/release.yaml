---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plex
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: plex
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: plex
        namespace: flux-system
